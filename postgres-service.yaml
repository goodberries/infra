apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: ClusterIP # Only accessible within the cluster
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
